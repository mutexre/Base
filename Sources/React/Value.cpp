#include <React/React.h>
/*
std::shared_ptr<SG::Value> SG::makeValue(const Rt::u1* ptr, Rt::u4 size, Rt::u4 count) {
    std::vector<Rt::u1> vec(size);
    memcpy(vec.data(), ptr, size * count);
    return std::make_shared<Value>(vec);
}
*/